
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: datarockets-personal-finance_development

test:
  <<: *default
  database: datarockets-personal-finance_test

production:
  <<: *default
  database: datarockets-personal-finance_production
  username: datarockets-personal-finance
  password: <%= ENV['DATAROCKETS_FINANCE_APP_DATABASE_PASSWORD'] %>
